{
  "version": "0.2.3",
  "license": "UNLICENSED",
  "scripts": {
    "release": "standard-version",
    "postinstall": "git config core.hooksPath .git-hooks"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
